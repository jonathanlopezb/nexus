module.exports = ({ env }) => ({
    'users-permissions': {
        config: {
            register: {
                allowedFields: ['PuntosNeurales', 'UrbanLevel'],
            },
        },
    },
});
